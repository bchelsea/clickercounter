
//declare the variable counter starts at 0
var counter = 0;
//write function that gets the counter and increments by one every "onClick"
function clicked(myFunction) {
    counter = counter + 1;
    //log out the click count
    console.log("click count: " + counter)
}